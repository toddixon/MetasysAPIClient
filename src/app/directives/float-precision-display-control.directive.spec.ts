import { FloatPrecisionDisplayControlDirective } from './float-precision-display-control.directive';

describe('FloatPrecisionDisplayControlDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatPrecisionDisplayControlDirective();
    expect(directive).toBeTruthy();
  });
});
