import { EnumStateTitlePipe } from './enum-state-title.pipe';

describe('EnumStateTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumStateTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
