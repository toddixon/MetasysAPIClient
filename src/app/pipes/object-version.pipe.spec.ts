import { ObjectVersionPipe } from './object-version.pipe';

describe('ObjectVersionPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectVersionPipe();
    expect(pipe).toBeTruthy();
  });
});
