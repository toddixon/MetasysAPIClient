import { OverrideExpirePipe } from './override-expire.pipe';

describe('OverrideExpirePipe', () => {
  it('create an instance', () => {
    const pipe = new OverrideExpirePipe();
    expect(pipe).toBeTruthy();
  });
});
